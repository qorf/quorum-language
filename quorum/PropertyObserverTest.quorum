use Libraries.Accessibility.all

class PropertyObserverTest is PropertyObserver
    action ReceiveEvent(PropertyEvent e)
        output "hello from PropertyObserver"
        output "Property = " + e:GetName()
    end
end