use Libraries.Containers.Array
class Main
    action Main
        Array<integer> ints
        ints:SetSize(4)
        ints:Set(0, 34)
        ints:Set(1, 16)
        ints:Set(2, -62)
        ints:Set(3, 3)
        integer i = ints:Get(0)
        integer j = ints:Get(1)
        integer k = ints:Get(2)
        integer l = ints:Get(3)

        output i
        output j
        output k
        output l
    end
end