use Libraries.Containers.HashTable
class Main
    action Main
        HashTable<text, integer> table
        table:Add("age", 23)
        table:Add("height", 65)
        
        text age = table:GetKey(23)
        text height = table:GetKey(65)

        output age
        output height
    end
end