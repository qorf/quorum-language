use Libraries.Containers.HashTable
class Main
    action Main
        HashTable<text, integer> table
        table:Add("age", 23)
        table:Add("height", 65)
        
        integer age = table:GetValue("age")
        integer height = table:GetValue("height")

        print age
        print height
    end
end