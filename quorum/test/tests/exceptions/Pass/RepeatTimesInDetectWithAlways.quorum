/*
Expected output:
detect
detect
always
*/

use Libraries.Language.Errors.all

class Main
    action Main
        check
            integer k = cast(integer, "3.7")
        detect e of type CastError
            integer z = 2
            repeat z times
                print "detect"
            end
        always
            print "always"
        end
    end
end