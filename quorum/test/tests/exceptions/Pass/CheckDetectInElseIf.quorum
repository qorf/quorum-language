/*
Expected output:
detect
*/

use Libraries.Language.Errors.all

class Main
    action Main
        integer z = 2 * 2
        if z = 5
            print "shouldn't be here"
        elseif z = 4
            check
                alert("detect")
            detect e
                print e:GetErrorMessage()
            end
        end
    end
end