/*
Expected output:
always
always
*/

use Libraries.Language.Errors.all

class Main
    action Main
        integer a = 2
        repeat a times
            check
            detect e
                print "shouldn't be here"
            always
                print "always"
            end
        end
    end
end