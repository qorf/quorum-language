/*
Expected output:
detect
*/

use Libraries.Language.Errors.all

class Main
    action Main
        integer k = 0
        repeat until k = 4
            if k > 2
                check
                    number f = cast(number, "nope")
                detect e
                    print "detect"
                end
            end
            k = k + 1
        end
    end
end