class Main
    action main
        if(foo(3, "yo") = 3)
            print "boo"
        end
        print "fubar"
    end

    action foo(number a, text b) returns integer
        return bar(a, b)
    end

    action bar(number a, integer b, text c) returns integer
        return a
    end
end