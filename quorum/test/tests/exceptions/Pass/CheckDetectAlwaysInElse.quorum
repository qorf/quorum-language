/*
Expected output:
detect
always
*/

use Libraries.Language.Errors.all

class Main
    action Main
        integer z = 2 * 2
        if z = 5
            print "shouldn't be here"
        else
            check
                alert("detect")
            detect e
                print e:GetErrorMessage()
            always
                print "always"
            end
        end
    end
end