class Main
    action Main
       integer a = multiply(multiply(multiply(multiply(2, 2), multiply(2, 2)), multiply(multiply(2, 2), multiply(2, 2))), 
                            multiply(multiply(multiply(2, 2), multiply(2, 2)), multiply(multiply(2, 2), multiply(2, 2))))
       output a
    end

    action multiply(integer a, integer b) returns integer
        return a * b
    end
end