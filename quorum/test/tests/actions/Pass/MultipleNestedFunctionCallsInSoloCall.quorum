class Main
    integer toPrint = 0

    action Main
        GetMultipliedResult(multiply(multiply(multiply(multiply(2, 2), multiply(2, 2)), multiply(multiply(2, 2), multiply(2, 2))),
                                     multiply(multiply(multiply(2, 2), multiply(2, 2)), multiply(multiply(2, 2), multiply(2, 2)))))
        print toPrint
    end

    action multiply(integer a, integer b) returns integer
        return a * b
    end

    action GetMultipliedResult(integer a)
        toPrint = a 
    end
end