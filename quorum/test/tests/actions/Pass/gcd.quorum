class Main
    action Main
       integer a = gcd(4, 2)
       boolean pass = a = 2
    end

    action gcd(integer a, integer b) returns integer
        if b = 0 then
            return a
        end else then
            return gcd(b, a mod b)
        end
    end
end