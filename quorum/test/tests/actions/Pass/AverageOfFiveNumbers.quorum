class Main
    action Main
       number a = average(153.4, 152.4, 446.53, 617.0, 318.4)
       print a
    end

    action average(number a, number b, number c, number d, number e) returns number
        number sum = a + b + c + d + e
        return sum / 5
    end
end