class Main
    action Main
       integer b = factorial(4)
       boolean pass = (4 * 3 * 2) = b
    end

    action factorial(integer num) returns integer
        if num <= 1 then
            return 1
        end else then
            return factorial(num - 1) * num
        end
    end
end