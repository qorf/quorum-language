use test.Support

class recursive
    action Main
            integer a = some_func(5)
            integer b = a + 5
            Support support
            integer a = support.do_something() + 10 * 5
    end

    action some_func(integer j) returns integer
        return j + 2
    end
end