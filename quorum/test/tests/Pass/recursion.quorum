class recursive
    action Main
        act recurse(3)
    end

    action recurse(integer val)
        if val > 0 then
            val = val - 1
            act recurse(val)
        end
    end
end