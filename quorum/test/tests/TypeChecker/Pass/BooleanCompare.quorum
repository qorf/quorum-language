use Libraries.Language.Support.CompareResult

class Main
    action Main
        Boolean bool1
        bool1:SetValue(true)
        Boolean bool2
        bool2:SetValue(true)
        CompareResult result = bool1:Compare(bool2)
        boolean final = result:result = result:EQUAL
    end
end