class Main
    Integer k = undefined
    Object o = undefined
    action Main
        // The boolean ensures that we made it to the end.
        integer t = -1
        Integer m
        k = m
        k:SetValue(5)
        o = cast(Object, k)
        Integer p = cast(Integer, o)
        t = p:GetValue()

        print t
    end
end