class Main
    Object o = undefined
    action Main
        // The boolean ensures that we made it to the end.
        boolean t = false
        Integer k
        k:SetValue(5)
        Object o
        o = cast(Object, k)
        t = true

        output t
    end
end