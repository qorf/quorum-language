class Main
    action Main
        // The boolean ensures that we made it to the end.
        integer t = 0
        Integer m
        m:SetValue(5)
        Object k = cast(Object, m)
        Integer o = cast(Integer, k)
        t = o:GetValue()

        print t
    end
end