class Main
    Integer k = undefined
    action Main
        // The boolean ensures that we made it to the end.
        boolean t = false
        Integer m
        k = m
        k:SetValue(5)
        Object o
        o = cast(Object, k)
        t = true

        print t
    end
end