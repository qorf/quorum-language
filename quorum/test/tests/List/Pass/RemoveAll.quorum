use Libraries.Containers.List

/**
 * Uses:
 * action RemoveAll( Type value )
**/

/**
 * Description:
**/

/**
 * Expected Results:
 * textTotal = "FiveNineEightThree"
**/

/**
 * @author Elliot Motl
**/
class Main
    action Main
       List<text> textList

       textList:Add("One")
       textList:Add("Five")
       textList:Add("Nine")
       textList:Add("One")
       textList:Add("Eight")
       textList:Add("Three")
       textList:Add("One")

       textList:RemoveAll("One")

       text textTotal = ""
       integer i = 0
       repeat from i to 3
           textTotal = textTotal + textList:Get(i)
           i = i + 1
       end
    end
end