use Libraries.Containers.List

/**
 * Uses:
 * action GetFirstIndexOf( Type value ) returns integer
 * action Add( Type value)
**/

/**
 * Description:
 * The purpose of this test case is to test the GetFirstIndexOf action.
**/

/**
 * Expected Results:
 * textIndex = 0
**/

/*
 * @author Elliot Motl
*/
class Main
    action Main
       List<text> textList

       textList:Add("one")
       textList:Add("five")
       textList:Add("nine")
       textList:Add("one")
       textList:Add("eight")
       textList:Add("three")
       textList:Add("one")

       integer textIndex = textList:GetFirstLocation("one")
    end
end