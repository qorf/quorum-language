use Libraries.Containers.Array
class Main
    action Main
        Text myText
        myText:SetValue("hello")
        text result = myText:GetCharacter(test(test(0,1), test(1 ,1)))
        print result
    end

    action test(integer b, integer n) returns Integer
        Integer i
        i:SetValue(b)
        return i
    end
end