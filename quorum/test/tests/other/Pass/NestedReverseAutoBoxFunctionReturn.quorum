use Libraries.Containers.Array
class Main
    action Main
        Text myText
        myText:SetValue("hello")
        text result = myText:GetCharacter(test(test(test(1))))
        print result
    end

    action test(integer n) returns Integer
        Integer i
        i:SetValue(n)
        return i
    end
end