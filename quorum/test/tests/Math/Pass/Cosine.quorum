use Libraries.Compute.Math

/**
 * Uses:
 * action Cosine(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the Cosine function in the Math class
**/

/**
 * Expected Results:
 * result = .87758
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:Cosine(0.5),5)
    end
end
