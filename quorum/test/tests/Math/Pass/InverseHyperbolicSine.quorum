use Libraries.Compute.Math

/**
 * Uses:
 * action InverseHyperbolicSine(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the InverseHyperbolicSine function in the Math class
**/

/**
 * Expected Results:
 * result = 0.88137
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:InverseHyperbolicSine(1.0),5)
        print result
    end
end
