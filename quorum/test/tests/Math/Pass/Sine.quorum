use Libraries.Compute.Math

/**
 * Uses:
 * action Sine(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the Sine function in the Math class
**/

/**
 * Expected Results:
 * result = 0.47943
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:Sine(0.5),5)
        print result
    end
end
