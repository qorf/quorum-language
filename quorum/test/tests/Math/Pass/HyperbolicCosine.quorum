use Libraries.Compute.Math

/**
 * Uses:
 * action HyperbolicCosine(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the HyperbolicCosine function in the Math class
**/

/**
 * Expected Results:
 * result = 1.12763
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:HyperbolicCosine(0.5),5)
        output result
    end
end
