use Libraries.Compute.Math

/**
 * Uses:
 * action Round(number value, boolean roundUp) returns number
 *
 * Description:
 * The purpose of this test case is to test the rounding capabilities of the Math class
 *
 * Expected Results:
 * result = 3.0
 **/

class Main
    action Main
        Math math
        number result = math:Round(3.5, false)
    end
end