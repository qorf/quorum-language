use Libraries.Compute.Math

/**
 * Uses:
 * action Logarithm(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the Logarithm function in the Math class
**/

/**
 * Expected Results:
 * result = 0.69897
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:Logarithm(5.0),5)
        print result
    end
end
