use Libraries.Compute.Math

/**
 * Uses:
 * action HyperbolicSine(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the HyperbolicSine function in the Math class
**/

/**
 * Expected Results:
 * result = 0.5211
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:HyperbolicSine(0.5),5)
        print result
    end
end
