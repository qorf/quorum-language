use Libraries.Compute.Math

/**
 * Uses:
 * system action SquareRoot(number num) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the Square Root function in the Math class
**/

/**
 * Expected Results:
 * result = 3.0
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:SquareRoot(9.0)
    end
end