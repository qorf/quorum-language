use Libraries.Compute.Math

/**
 * Uses:
 * action Tangent(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the Tangent function in the Math class
**/

/**
 * Expected Results:
 * result = 0.5463
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:Tangent(0.5),5)
    end
end
