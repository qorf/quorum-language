use Libraries.Compute.Math

/**
 * Uses:
 * action InverseHyperbolicTangent(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the InverseHyperbolicTangent function in the Math class
**/

/**
 * Expected Results:
 * result = 0.54931
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:InverseHyperbolicTangent(0.5),5)
        print result
    end
end
