use Libraries.Compute.Math

/**
 * Uses:
 * action InverseSine(integer numb) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the InverseSine function in the Math class
**/

/**
 * Expected Results:
 * result = 0.5236
**/

/*
 * @author James Daniel
*/
class Main
    action Main
        Math math
        number result = math:Round(math:InverseSine(0.5),5)
        print result
    end
end
