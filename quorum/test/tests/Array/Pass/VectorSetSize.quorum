use Libraries.Containers.Array

class Main
    action main
        Array<integer> a
        a:SetSize(3)
        a:Add(12)
        a:Add(12)
        a:Add(12)

        integer result1 = a:GetSize()
        print result1
    end
end