use Libraries.Containers.Array

class Main
    action main
        Array<integer> a
        a:SetAutoResize(false)
        a:SetSize(3)
        a:Add(12)
        a:Add(12)
        a:Add(12)

        integer result1 = a:GetSize()
    end
end