use Libraries.System.File
use Libraries.System.DateTime
use Libraries.Containers.Blueprints.Indexed
class Main
    action Main
        File testDir
        testDir:SetRelativePath("../../../test/tests/File/Resources/DirectoryToList")
        Indexed<File> list = testDir:GetDirectoryListing()

        // Make sure all the files we expect are here. (should be 5)
        File f1 = list:Get(0)
        File f2 = list:Get(1)
        File f3 = list:Get(2)
        File f4 = list:Get(3)
        File f5 = list:Get(4)

        boolean success = f1:GetFileName() = ".svn" and f2:GetFileName() = "a" and f3:GetFileName() = "b" and f4:GetFileName() = "c" and f5:GetFileName() = "empty"

        print success
    end
end