package Libraries.Robots

/*
The Button class represents the different touch screen buttons and the Black button 
present on the controller. 
There are seven different buttons present. The A button, B button, Black button,
LeftArrow button, RightArrow button, UpArrow button, and DownArrow button.

Attribute: Author Sahana Tambi Sathyanarayana

Attribute: Example

use Libraries.Robots.Button

// Declaraing the Button
Button button
if button:IsAButtonPressed() then
    output "Button A is Pressed"
end
  
*/

class Button 
 /*
        This action tells if A button on the controller is pressed or not.

        Attribute: Returns true if A button is pressed otherwise returns
        false

        Attribute: Example
        
        Button button
        if button:IsAButtonPressed() not= true then
            output "A Button is not Pressed"
        end
    */
    system action IsAButtonPressed() returns boolean

/*
        This action tells if B button on the controller is pressed or not.

        Attribute: Returns true if B button is pressed otherwise returns
        false

        Attribute: Example
        
        Button button
        if button:IsBButtonPressed() not= true then
            output "B Button is not Pressed"
        end
*/
    system action IsBButtonPressed() returns boolean

/*
        This action tells if Black button on the controller is pressed or not.

        Attribute: Returns true if Black button is pressed otherwise returns
        false
        
        Attribute: Example
        
        Button button
        if button:IsBlackButtonPressed() not= true then
            output "Black Button is not Pressed"
        end
*/
    system action IsBlackButtonPressed() returns boolean

/*
        This action tells if LeftArrow button on the controller is pressed or not.

        Attribute: Returns true if LeftArrow button is pressed otherwise returns
        false
        
        Attribute: Example
        
        Button button
        if button:IsLeftArrowPressed() not= true then
            output "Left Arrow is not Pressed"
        end
*/
    system action IsLeftArrowPressed() returns boolean

/*
        This action tells if RightArrow button on the controller is pressed or not.

        Attribute: Returns true if RightArrow button is pressed otherwise returns
        false
        
        Attribute: Example
        
        Button button
        if button:IsRightArrowPressed() not= true then
            output "Right Arrow is not Pressed"
        end
*/
    system action IsRightArrowPressed() returns boolean

/*
        This action tells if UpArrow button on the controller is pressed or not.

        Attribute: Returns true if UpArrow button is pressed otherwise returns
        false
        
        Attribute: Example
        
        Button button
        if button:IsUpArrowPressed() not= true then
            output "Up Arrow is not Pressed"
        end
*/
    system action IsUpArrowPressed() returns boolean

/*
        This action tells if DownArrow button on the controller is pressed or not.

        Attribute: Returns true if DownArrow button is pressed otherwise returns
        false
        
        Attribute: Example
        
        Button button
        if button:IsDownArrowPressed() not= true then
            output "Down Arrow is not Pressed"
        end
*/
    system action IsDownArrowPressed() returns boolean
end
