package Libraries.Accessibility

/*
    The FocusObserver class provides functionality to output any focus system 
    event.

    Attribute: Example

    AccessibilityManager manager
    FocusObserver fObserver
    manager:Add(fObserver)

    manager:Start()
    manager:Stop()

*/

class FocusObserver 

    /*
        This action is a blueprint function that recieves an event from the system
        for the programmer to use.

        Attribute: Parameter event The event from the system.

        Attribute: Example

        KeyboardObserver observer
        SystemEvent event
        observer:RecieveEvent(event)
    */
    blueprint action RecieveEvent(AccessibilityEvent event)

end