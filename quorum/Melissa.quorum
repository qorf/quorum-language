use Libraries.Containers.Array
class Melissa

    action Main
        Array<integer> a
    end

    /*action test(Object o) returns boolean
        repeat 2 times
            Object i
            if(i:Equals(o))then
                return true
            end else then
                return false
            end
        end
        return false
    end*/
end