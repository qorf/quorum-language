use Libraries.Sound.Music
use Libraries.Sound.Chord
use Libraries.Containers.Array

class Melissa
    Array<Chord> a
    action Main
        Chord c
        c:Add(60)
        c:Add(64)
        c:Add(67)

        a:Add(c)

        integer i = a:GetSize()
        repeat from a:GetSize() to GetSomething(a:GetSize()) 
            print "something"
        end
        
    end
    action GetSomething(integer val) returns integer
        return val
    end
end