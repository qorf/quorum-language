/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package org.sodbeans.phonemic;

/**
 * Defines the two available types of speech requests -- TEXT (for strings)
 * and CHARACTER (for speaking single characters).
 *
 * @author jeff
 */
public enum RequestType {
    TEXT,
    CHARACTER;
}
