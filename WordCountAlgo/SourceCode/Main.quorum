use Libraries.Containers.HashTable
use Libraries.Containers.Iterator

WordFrequency a
a:SetFilePath("input1.txt")
a:Calculate()
a:PrintFrequencies()
HashTable<text, integer> freq = a:GetFrequencyMap()
Iterator<text> itr = freq:GetKeyIterator()
repeat while itr:HasNext()
    output itr:Next()
end
