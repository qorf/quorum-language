use Libraries.Containers.List 
use Libraries.Containers.Blueprints.Iterator 
use Libraries.Containers.Array

class Main
    List<Array<integer>> list 

    action Main 
        Iterator<integer> iterator = list:GetIterator() 
        integer x = 0 
        x = iterator:Next() 
    end
end