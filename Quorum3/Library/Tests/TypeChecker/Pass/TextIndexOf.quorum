class Main
    action Main
        text p = "The force is strong with this one."
        Text t 
        t:SetValue(p)

        output p:IndexOf("force")
        output t:IndexOf("force")
        output p:IndexOf("Yoda")
        output t:IndexOf("Yoda")
    end
end