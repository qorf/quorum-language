class Main
    action Main
        text p = ""
        Text t 
        t:SetValue(p)
        
        text str = ""
        str = "start" + p:GetLineFeed() + "end"
        output str

        str = "start" + t:GetLineFeed() + "end"
        output str
    end
end