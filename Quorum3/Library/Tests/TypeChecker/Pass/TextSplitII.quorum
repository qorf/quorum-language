use Libraries.Containers.Array

class Main
    action Main
        text p = "1,2,3,4"
        Text t 
        t:SetValue(p)
        
        Array<text> result = p:Split(",")
        output result:Get(0)
        output result:Get(1)
        output result:Get(2)
        output result:Get(3)
        result = t:Split(",")
        output result:Get(0)
        output result:Get(1)
        output result:Get(2)
        output result:Get(3)
    end
end