use Libraries.Compute.Math

/**
 * Uses:
 * action Round(number value, integer decimalPlace, boolean roundUp) returns number
 *
 * Description:
 * The purpose of this test case is to test the rounding capabilities of the Math class
 *
 **/

class Main
    action Main
        Math math
        number result1 = math:Round(3.55, 1, false)
        number result2 = math:Round(3.55, 1, true)
        output result1
        output result2
        output math:Round(math:pi, 1, false)
        output math:Round(math:pi, 2, false)
        output math:Round(math:pi, 3, false)
        output math:Round(math:pi, 4, false)
        output math:Round(math:pi, 5, false)
        output math:Round(math:pi, 6, false)

    end
end