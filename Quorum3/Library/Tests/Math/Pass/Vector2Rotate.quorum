use Libraries.Compute.Vector2
use Libraries.Compute.Math

class Vector2Rotate 
    action Main
        Math math
        Vector2 vector
        vector:Set(7, 2)
        output vector:Rotate(30):ToText()
        vector:Set(7, 2)
        output vector:RotateRadians(math:pi / 6):ToText()
        vector:Set(7, 2)
        output vector:Rotate90Degrees(1):ToText()
    end
end