class Main
    action Main
        Integer one
        one:SetValue(66)
        integer two = 66
        Yoda<Integer, integer, text> yoda
        check
            text ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
