class Main
    action Main
        text one = "green"
        text two = "green"
        Yoda<text, text, boolean> yoda
        check
            boolean ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
