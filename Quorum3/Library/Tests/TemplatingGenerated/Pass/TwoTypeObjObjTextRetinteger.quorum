class Main
    action Main
        Text one
        one:SetValue("green")
        Text two
        two:SetValue("green")
        Yoda<Text, Text, integer> yoda
        check
            integer ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
