class Main
    action Main
        Boolean one
        one:SetValue(true)
        Boolean two
        two:SetValue(true)
        Yoda<Boolean, Boolean, Boolean> yoda
        Boolean ret = yoda:useForce(one, two)
        output "passed"
     end
end
