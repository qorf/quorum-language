class Main
    action Main
        Text one
        one:SetValue("green")
        text two = "green"
        Yoda<Text, text, text> yoda
        check
            text ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
