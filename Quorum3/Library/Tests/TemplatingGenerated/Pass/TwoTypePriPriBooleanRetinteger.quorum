class Main
    action Main
        boolean one = true
        boolean two = true
        Yoda<boolean, boolean, integer> yoda
        check
            integer ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
