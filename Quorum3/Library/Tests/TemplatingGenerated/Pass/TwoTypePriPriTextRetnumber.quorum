class Main
    action Main
        text one = "green"
        text two = "green"
        Yoda<text, text, number> yoda
        check
            number ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
