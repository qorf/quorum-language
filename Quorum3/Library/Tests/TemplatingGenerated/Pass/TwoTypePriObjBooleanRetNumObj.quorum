class Main
    action Main
        boolean one = true
        Boolean two
        two:SetValue(true)
        Yoda<boolean, Boolean, Number> yoda
        Number ret = yoda:useForce(one, two)
        output "passed"
     end
end
