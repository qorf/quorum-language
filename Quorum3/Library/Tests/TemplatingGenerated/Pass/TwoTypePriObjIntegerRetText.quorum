class Main
    action Main
        integer one = 66
        Integer two
        two:SetValue(66)
        Yoda<integer, Integer, text> yoda
        check
            text ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
