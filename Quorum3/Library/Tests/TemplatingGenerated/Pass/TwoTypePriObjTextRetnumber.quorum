class Main
    action Main
        text one = "green"
        Text two
        two:SetValue("green")
        Yoda<text, Text, number> yoda
        check
            number ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
