class Main
    action Main
        integer one = 66
        Integer two
        two:SetValue(66)
        Yoda<integer, Integer, integer> yoda
        boolean wrongRet = yoda(one, two)
     end
end
