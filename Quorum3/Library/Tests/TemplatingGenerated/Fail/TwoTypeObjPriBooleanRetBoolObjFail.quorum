class Main
    action Main
        Boolean one
        one:SetValue(true)
        boolean two = true
        Yoda<Boolean, boolean, Boolean> yoda
        integer wrongRet = yoda(one, two)
     end
end
