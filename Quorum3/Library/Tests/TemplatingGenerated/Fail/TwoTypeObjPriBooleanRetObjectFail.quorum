class Main
    action Main
        Boolean one
        one:SetValue(true)
        boolean two = true
        Yoda<Boolean, boolean, Object> yoda
        boolean wrongRet = yoda(one, two)
     end
end
