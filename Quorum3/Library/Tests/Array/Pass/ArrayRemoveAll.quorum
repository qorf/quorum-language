use Libraries.Containers.Array

class Main
    action Main
        Array<integer> a
        a:SetAutoResize(false)
        a:Add(12)
        a:Add(13)
        a:Add(13)

        a:RemoveAll(13)

        integer result1 = a:Get(0)
        integer size = a:GetSize()
        output result1
        output size
    end
end