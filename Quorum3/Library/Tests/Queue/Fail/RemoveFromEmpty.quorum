use Libraries.Containers.Queue

class Main
    action Main
       Queue<text> s

       text result = ""
       integer i = 0
       repeat 5 times 
            result = result + s:RemoveFromFront()
            i = i + 1
       end
    end
end