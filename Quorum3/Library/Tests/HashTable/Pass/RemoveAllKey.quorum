use Libraries.Containers.HashTable
class Main
    action Main
        HashTable<text, integer> table
        table:Add("age", 23)
        table:Add("age", 32)
        table:Add("height", 65)
        
        boolean age = table:RemoveAllKeys("age")

        output age
    end
end