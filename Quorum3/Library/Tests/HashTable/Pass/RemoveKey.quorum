use Libraries.Containers.HashTable
class Main
    action Main
        HashTable<text, integer> table
        table:Add("age", 23)
        table:Add("height", 65)
        
        integer age = table:RemoveKey("age")

        output age
    end
end