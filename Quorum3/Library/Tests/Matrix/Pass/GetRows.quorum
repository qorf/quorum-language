use Libraries.Compute.Matrix

class Main 
    action Main
        Matrix matrix
        matrix:SetSize(2, 3)
        CheckResult(matrix:GetRows())
    end

    action CheckResult(integer result)
        integer answer = 2
        if answer = result
            output "pass"
        else
            output "fail"
        end
    end
end