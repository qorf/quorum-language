/*
Expected output:
detect
always
*/

class Main
    action Main
        integer z = 2 * 2
        if z = 4
            check
                alert("detect")
            detect e
                output e:GetErrorMessage()
            always
                output "always"
            end
        else
            output "shouldn't be here"
        end
    end
end