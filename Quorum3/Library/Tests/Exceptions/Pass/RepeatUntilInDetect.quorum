/*
Expected output:
detect
detect
*/

use Libraries.Language.Errors.CastError

class Main
    action Main
        check
            integer k = cast(integer, "3.7")
        detect e is CastError
            integer z = 0
            repeat until z = 2
                output "detect"
                z = z + 1
            end
        end
    end
end