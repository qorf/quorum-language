use Libraries.Language.Errors.Error

class Main 
    action Main
        check
            output "Try Block"
            //Error error
            //alert(error)
        detect bob
            output "Catch Block"
        end
    end
end