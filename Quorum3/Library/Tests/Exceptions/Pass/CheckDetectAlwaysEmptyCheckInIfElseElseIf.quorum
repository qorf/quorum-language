/*
Expected output:
always
*/

class Main
    action Main
        integer a = 2
        integer b = 2
        boolean k = (a = b)
        if k
            check
            detect e
                output "shouldn't be here"
            always
                output "always"
            end
        elseif (2 * 2 = 5)
            check
            detect e
                output "shouldn't be here"
            always
                output "always"
            end
        else
            check
            detect e
                output "shouldn't be here"
            always
                output "always"
            end
        end
    end
end