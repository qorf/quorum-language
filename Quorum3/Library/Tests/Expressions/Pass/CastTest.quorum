use Libraries.Compute.Math

class Main
    Math math
    ATAN2_COUNT = 2
    constant number ATAN2_DIM = cast(integer, math:SquareRoot(ATAN2_COUNT))
    action Main
        output ATAN2_DIM
    end
end

