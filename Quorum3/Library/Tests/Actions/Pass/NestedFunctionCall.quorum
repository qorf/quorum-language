class Main
    action Main
       integer a = multiply(multiply(3, 5), multiply(4, 5))
       output a
    end

    action multiply(integer a, integer b) returns integer
        return a * b
    end
end