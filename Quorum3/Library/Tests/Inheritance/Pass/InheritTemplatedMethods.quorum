class Main
    action Main
        DTemplated<Integer> d
        CTemplated<Number> c
        BTemplated<Text> b
        ATemplated<Boolean> a

        boolean val = true
        a:set(val)
        boolean bool = a:get()

        text t = "true"
        b:set(t)
        text tResult = b:get()

        Number num
        num:SetValue(1.2)
        c:set(num)
        Number n = c:get()

        number nResult = n:GetValue()

        Integer int
        int:SetValue(10)
        d:set(int)
        integer intResult = d:get()

        output bool
        output tResult
        output nResult
        output intResult
    end
end