use Libraries.Containers.Array

class Main 
    action Main
        Array<CastA> values
        GetCast(values)

        CastA a = values:Get(0)
        output "hi"
    end

    action GetCast(Array<CastA> vals)
        CastB b
        vals:Add(b)
    end
end