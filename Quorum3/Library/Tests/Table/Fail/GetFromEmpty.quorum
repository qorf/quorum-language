use Libraries.Containers.Table

class Main
    action main
        Table<integer> a
        a:Empty()
        boolean r1 = a:IsEmpty()
        a:Add(0,0,12)
        a:Add(0,0,13)
        a:Add(0,0,14)

        a:Empty()
        boolean r2 = a:IsEmpty()
        integer r3 = a:GetFromFrontOfRow()

        output r1
        output r2
        output r3
    end
end