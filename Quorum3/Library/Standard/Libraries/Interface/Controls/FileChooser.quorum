package Libraries.Interface.Controls

use Libraries.System.File

class FileChooser 
    action OpenFileDialog(File location, File filer) returns File
        text result = OpenFileDialogNative(undefined, "")
        output result
        return undefined
    end

    system action OpenFileDialogNative(text path, text filter) returns text
end