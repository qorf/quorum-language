package Libraries.Robots.Lego

class TouchSensor
    system action SetPort(integer portNumber)
    system action IsPressed returns boolean
    //RobotC has a method to keep count of the times pressed
end