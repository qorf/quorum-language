package Libraries.Game.Behaviors

use Libraries.Interface.Item
use Libraries.Interface.TextBox

class TextBoxCaretStopBehavior is Behavior
    action Begin(Item item)
        /*
        For testing purposes only. This wipes out all behaviors on the Item, not
        just the movement ones, and thus will not work as a long term solution.
        */
        item:CancelBehaviors()
    end
end