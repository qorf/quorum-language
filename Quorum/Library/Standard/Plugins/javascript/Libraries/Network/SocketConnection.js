function plugins_quorum_Libraries_Network_SocketConnection_() {

    this.Open$quorum_text$quorum_integer = function (hostname, port) {
        let exceptionInstance_ = new quorum_Libraries_Language_Errors_Error_();
        exceptionInstance_.SetErrorMessage$quorum_text("The Socket library is not available on the web");
        throw exceptionInstance_;
    };

    this.Write$quorum_text = function (message) {
        let exceptionInstance_ = new quorum_Libraries_Language_Errors_Error_();
        exceptionInstance_.SetErrorMessage$quorum_text("The Socket library is not available on the web");
        throw exceptionInstance_;
    };

    this.WriteBytes$quorum_Libraries_Containers_ByteArray_ = function () {
        let exceptionInstance_ = new quorum_Libraries_Language_Errors_Error_();
        exceptionInstance_.SetErrorMessage$quorum_text("The Socket library is not available on the web");
        throw exceptionInstance_;
    };

    this.Read$quorum_integer = function () {
        let exceptionInstance_ = new quorum_Libraries_Language_Errors_Error_();
        exceptionInstance_.SetErrorMessage$quorum_text("The Socket library is not available on the web");
        throw exceptionInstance_;
    };

    this.ReadBytes$quorum_integer = function () {
        let exceptionInstance_ = new quorum_Libraries_Language_Errors_Error_();
        exceptionInstance_.SetErrorMessage$quorum_text("The Socket library is not available on the web");
        throw exceptionInstance_;
    };

    this.ReadLine = function () {
        let exceptionInstance_ = new quorum_Libraries_Language_Errors_Error_();
        exceptionInstance_.SetErrorMessage$quorum_text("The Socket library is not available on the web");
        throw exceptionInstance_;
    };

    this.Close = function () {
        let exceptionInstance_ = new quorum_Libraries_Language_Errors_Error_();
        exceptionInstance_.SetErrorMessage$quorum_text("The Socket library is not available on the web");
        throw exceptionInstance_;
    };

}

