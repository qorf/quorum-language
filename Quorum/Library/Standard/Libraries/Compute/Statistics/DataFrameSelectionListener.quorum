package Libraries.Compute.Statistics

/*
    This is a general class for listening to the selection changes in a DataFrame.
*/
class DataFrameSelectionListener
    /* Whenever the selection of a frame changes, the original frame is sent back. */
    blueprint action SelectionChanged(DataFrame frame)
end