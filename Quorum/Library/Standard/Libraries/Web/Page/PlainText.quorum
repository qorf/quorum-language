package Libraries.Web.Page
use Libraries.Web.Page.WebTag

class PlainText is WebTag
    private text plainText = ""

    action Generate() returns text
        return plainText
    end

    action SetText(text t)
        plainText = t
    end
end