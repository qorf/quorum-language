package Libraries.Interface.Events

class GutterListener

    /*
    The GutterClicked event is called whenever a Gutter is clicked, so long as
    this GutterListener was registered with the Gutter's AddGutterListener
    action.
    */
    action GutterClicked(GutterEvent event)
        
    end
end