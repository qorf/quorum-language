package Libraries.Interface.Events
use Libraries.Containers.Array

class GestureProcessor
    // Currently only supported on the web
    // takes the id of the canvas will be contained in 
    system action InitializeGestureProcessor(text containerID)
end

