package Libraries.Game.Graphics.Models
use Libraries.Compute.Matrix4
use Libraries.Containers.Array
class AnimationFrame
    Array<Matrix4> joints

    action GetJoints returns Array<Matrix4>
        return joints
    end

    action SetJoints(Array<Matrix4> joints)
        me:joints = joints
    end
end