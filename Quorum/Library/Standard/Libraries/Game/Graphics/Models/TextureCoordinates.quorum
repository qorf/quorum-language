package Libraries.Game.Graphics.Models

use Libraries.Containers.Number32BitArray
class TextureCoordinates
    
Number32BitArray array
    
    action GetArray returns Number32BitArray
        return array
    end

    action SetArray(Number32BitArray array)
        me:array = array
    end
end