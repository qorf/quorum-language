package Libraries.Game.Graphics

use Libraries.Interface.Item2D

class Painter2D
    
    blueprint action SetCamera(Camera camera)
    
    blueprint action Begin
    
    blueprint action End
    
    blueprint action SetClipping(boolean clippingEnabled)
    
    blueprint action SetColor(Color color)
    
    blueprint action SetColor(number red, number green, number blue, number alpha)
    
    blueprint action QueueForDrawing(Item2D item)
    
    blueprint action Draw(Drawable drawable)
    
    blueprint action SetClipCoordinates(number x, number y, number width, number height)
    
end