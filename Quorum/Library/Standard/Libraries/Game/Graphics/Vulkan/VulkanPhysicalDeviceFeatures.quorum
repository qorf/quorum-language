package Libraries.Game.Graphics.Vulkan

class VulkanPhysicalDeviceFeatures
    
    
/*This action destroys this data structure and releases all system resources related to it. Once the data is disposed, it can't
be used anymore, so this should only be done to clean up resources when the data is no longer needed.*/
    system action Dispose
end