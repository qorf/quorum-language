package Libraries.Game.Graphics.Vulkan

class VulkanDrawIndirectData
    integer vertexCount = 0
    integer instanceCount = 0
    integer firstVertex = 0
    integer firstInstance = 0
    
    action GetVertexCount returns integer
        return vertexCount
    end

    action SetVertexCount(integer vertexCount)
        me:vertexCount = vertexCount
    end

    action GetInstanceCount returns integer
        return instanceCount
    end

    action SetInstanceCount(integer instanceCount)
        me:instanceCount = instanceCount
    end

    action GetFirstVertex returns integer
        return firstVertex
    end

    action SetFirstVertex(integer firstVertex)
        me:firstVertex = firstVertex
    end

    action GetFirstInstance returns integer
        return firstInstance
    end

    action SetFirstInstance(integer firstInstance)
        me:firstInstance = firstInstance
    end
    
    /*This action returns the size of a single VulkanDrawIndirectData data structure, as it needs to stored
in a VulkanBuffer for use in indirect draw commands.*/
    system action GetByteSize returns integer
    
end