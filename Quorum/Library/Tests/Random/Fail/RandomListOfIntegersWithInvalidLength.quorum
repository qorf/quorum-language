/**
 * Libraries.Compute.Random test case
 *
 * Tests the RandomListOfIntegers(size) function. Tests with a known
 * seed (1101102342).
 *
 * Expected result: An OutOfBoundsError will be thrown because the given number
 * is <= 0.
 */

use Libraries.Compute.Random

class Main
    action Main
        Random random
        random:SetSeed(1101102342)
        random:RandomListOfIntegers(0)
    end
end