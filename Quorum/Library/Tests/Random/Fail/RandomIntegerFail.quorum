/**
 * Libraries.Compute.Random test case
 *
 * Tests the RandomInteger() function. Tests with a known seed (1101102342).
 *
 * Expected result: An OutOfBoundsError will be thrown because the given
 * maximum number is not >= 1.
 */

use Libraries.Compute.Random

class Main
    action Main
        Random random
        random:SetSeed(1101102342)
        integer result = random:RandomInteger(0)
    end
end