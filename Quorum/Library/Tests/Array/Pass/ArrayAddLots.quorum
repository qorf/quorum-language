use Libraries.Containers.Array

class Main
    action Main
        Array<integer> a
        i = 0
        repeat 1000 times
            a:Add(i)
            output i
        end

        output "Finished"
    end
end