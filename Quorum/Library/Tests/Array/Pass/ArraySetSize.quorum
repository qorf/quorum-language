use Libraries.Containers.Array

class Main
    action Main
        Array<integer> a
        a:SetAutoResize(false)
        a:SetSize(3)
        a:Set(0, 12)
        a:Set(0, 12)
        a:Set(0, 12)

        integer result1 = a:GetSize()
        output result1
    end
end