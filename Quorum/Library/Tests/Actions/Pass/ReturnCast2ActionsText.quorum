class Main
    action Main
        text a = ""
        x = foo(a)
        output x
        output bar(x)
    end

    action foo (text a) returns text
        return a + "foo"
    end

    action bar (text a) returns text
        return a + "bar"
    end
end