class Main
    action Main
        text outputStr = "...Liftoff"
        integer i = 1
        repeat 3 times
            outputStr = addBeg(outputStr, i)
            i = i + 1
        end
        output outputStr
    end
 
    action addBeg(text str, integer i) returns text
        return i + str
    end
end
