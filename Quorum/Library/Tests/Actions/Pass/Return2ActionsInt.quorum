class Main
    action Main
        integer a = -2
        output bar(foo(a))
    end

    action foo (integer a) returns integer
        a = a * -1
        return a
    end

    action bar(integer a) returns integer
        a = a * 10
        return a
    end
end