use Libraries.Compute.Vector2

class Vector2Length 
    action Main
        Vector2 vector
        vector:Set(3, 4)
        output vector:Length(3, 4)
        output vector:Length()
        output vector:LengthSquared(3, 4)
        output vector:LengthSquared()
        
    end
end