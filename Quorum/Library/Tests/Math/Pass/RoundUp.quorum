use Libraries.Compute.Math

/**
 * Uses:
 * action Round(number value) returns number
 **/

/**
 * Description:
 * The purpose of this test case is to test the rounding capabilities of the Math class
**/

/**
 * Expected Results:
 * result = 3.0
**/

class Main
    action Main
        Math math
        number result = math:Round(2.96)
        output result
        output math:Round(math:pi, 1)
        output math:Round(math:pi, 2)
        output math:Round(math:pi, 3)
        output math:Round(math:pi, 4)
        output math:Round(math:pi, 5)
    end
end