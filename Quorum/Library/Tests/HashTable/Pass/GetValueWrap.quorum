use Libraries.Containers.HashTable
class Main
    action Main
        HashTable<Integer, Integer> hash

        hash:Add(0,9)
        Integer hasWrap = hash:GetValue(8)
        if hasWrap = undefined
            output "undefined"
        else
            output "exists"
        end
    end
end