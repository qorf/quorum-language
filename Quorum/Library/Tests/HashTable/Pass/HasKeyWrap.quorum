use Libraries.Containers.HashTable
class Main
    action Main
        HashTable<integer, integer> hash

        hash:Add(0,9)
        hasWrap = hash:HasKey(8)

        //this should return false
        output hasWrap
    end
end