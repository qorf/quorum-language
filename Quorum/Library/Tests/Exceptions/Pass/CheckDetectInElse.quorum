/*
Expected output:
detect
*/

class Main
    action Main
        integer z = 2 * 2
        if z = 5
            output "shouldn't be here"
        else
            check
                alert("detect")
            detect e
                output e:GetErrorMessage()
            end
        end
    end
end