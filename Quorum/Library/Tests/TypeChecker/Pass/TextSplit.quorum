use Libraries.Containers.Array

class Main
    action Main
        text p = "The force is strong with this one."
        Text t 
        t:SetValue(p)
        
        Array<text> result = p:Split(" ")
        output result:Get(0)
        output result:Get(1)
        output result:Get(2)
        output result:Get(3)
        output result:Get(4)
        output result:Get(5)
        output result:Get(6)
        result = t:Split(" ")
        output result:Get(0)
        output result:Get(1)
        output result:Get(2)
        output result:Get(3)
        output result:Get(4)
        output result:Get(5)
        output result:Get(6)
    end
end