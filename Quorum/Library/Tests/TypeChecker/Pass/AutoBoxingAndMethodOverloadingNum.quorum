class Main
    action Main
        number x = 4.0
        number result = say_stuff(x)
        output result
    end

    action say_stuff(number n) returns number
        return 1.0
    end

    action say_stuff(integer i) returns integer
        return 2
    end
end