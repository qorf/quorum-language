class Main
    action Main
        text p = "The force is strong with this one."
        Text t 
        t:SetValue(p)

        output p:IndexOf("force", 2)
        output t:IndexOf("force", 2)
        output p:IndexOf("force", 10)
        output t:IndexOf("force", 10)
        output p:IndexOf("th", 0)
        output t:IndexOf("th", 0)
        output p:IndexOf("th", 23)
        output t:IndexOf("th", 23)
    end
end