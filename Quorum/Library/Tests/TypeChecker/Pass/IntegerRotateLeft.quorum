class Main
    action Main
        Integer t 
        t:SetValue(381)
        integer p = 381

        output t:RotateLeft(2)
        output p:RotateLeft(2)
        
        p = p - 1
        t:SetValue(p)
        output t:RotateLeft(2)
        output p:RotateLeft(2)
    end
end