use Libraries.Language.Support.CompareResult

class Main
    action Main
        Integer var1
        var1:SetValue(-13)
        Integer var2
        var2:SetValue(450)
        CompareResult result = var1:Compare(var2)
        boolean final = result:result = result:SMALLER
        output final
    end
end