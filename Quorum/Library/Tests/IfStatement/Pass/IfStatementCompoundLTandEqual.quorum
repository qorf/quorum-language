class Main
    action Main
        boolean printBool = false
        integer a = 45
        integer b = 21
        integer c = 5
        integer d = 3
        integer e = 10
        integer f = 12
        integer g = 7

        if (a + b) mod (c * d) / (f - e) < g and a / (c * d * d) = 1
            printBool = true
        end

        output printBool
    end
end

// output true