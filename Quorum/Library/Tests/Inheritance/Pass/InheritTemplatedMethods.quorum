class Main
    action Main
        DTemplated<integer> d
        CTemplated<number> c
        BTemplated<text> b
        ATemplated<boolean> a

        boolean val = true
        a:set(val)
        boolean bool = a:get()

        text t = "true"
        b:set(t)
        text tResult = b:get()

        number num = 1.2
        c:set(num)
        number n = c:get()

        number nResult = n

        integer int = 10
        d:set(int)
        integer intResult = d:get()

        output bool
        output tResult
        output nResult
        output intResult
    end
end