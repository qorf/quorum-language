/*
Expected Output:
*/

class Main
    action Main
        Returner<integer> r
        integer i = 1
        repeat 5 times
            r:ShowThese(Two() * One(), r:ReturnThis(One() * 1000))
            i = i + 1
        end
        output "passed"
    end

    action One returns integer
        return 1
    end

    action Two returns integer
        return 2
    end
end