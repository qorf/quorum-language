use Libraries.Compute.Matrix

class Main 
    action Main
        Matrix matrix
        matrix:SetSize(2, 3)
        CheckResult(matrix:GetColumns())
    end

    action CheckResult(integer result)
        integer answer = 3
        if answer = result
            output "pass"
        else
            output "fail"
        end
    end
end