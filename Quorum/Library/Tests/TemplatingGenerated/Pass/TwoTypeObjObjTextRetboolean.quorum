class Main
    action Main
        Text one
        one:SetValue("green")
        Text two
        two:SetValue("green")
        Yoda<Text, Text, boolean> yoda
        check
            boolean ret = yoda:useForce(one, two)
        detect e
            output "passed"
        end
     end
end
