use Libraries.Containers.List

/**
 * Uses:
 * action IsEmpty() returns boolean
 * action Add( Type value)
**/

/**
 * Description:
 * The purpose of this test case is to test the IsEmpty action of the
 * LinkedList class. Creates empty lists and tests if they are empty.
 * also tests if one is not empty
**/

/**
 * Expected Results:
       boolean bool1 = true
       boolean bool2 = false
**/

/**
 * @author Elliot Motl
**/

class Main
    action Main
       List<text> textList

       boolean bool1 = textList:IsEmpty()
       textList:Add("hi")
       boolean bool2 = textList:IsEmpty()

       output bool1
       output bool2
    end
end