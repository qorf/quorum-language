package Science.Tests

use Libraries.Testing.Test
use Libraries.Compute.Statistics.DataFrame
use Libraries.Compute.Statistics.Tests.CompareGroups
use Libraries.Compute.Statistics.Reporting.CompareGroupsResult

/*
    This class tests a one-sample wilcoxon signed-rank test
*/
class CompareRankedGroupToMedianTest is Test

    on create
        SetName("Calculate CompareRankedGroupToMedian")
    end

    action Run
        ColumnCalculationTest framer
        DataFrame frame = framer:GetDefaultDataFrame()

        CompareGroups compare
        compare:Ranked(true)
        compare:AddColumn(0)
        frame:Calculate(compare)
        
        CompareGroupsResult result = compare:GetResult()
        Check(result:GetTestStatistic(), 2.096569673443837)
        Check(result:GetProbabilityValue(), 0.03603168621823327)
        Check(result:GetEffectSize(), 0.855920985021826)
    end
end